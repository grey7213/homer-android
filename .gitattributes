# web-patches 里的补丁必须按字节保存。CRLF 转换会让 git apply 报
# "corrupt patch" 或算错行尾，三方合并直接失败。
*.patch -text

# gradlew 是 shell 脚本，CI 在 Linux 上跑它，行尾必须是 LF。
gradlew text eol=lf
*.bat text eol=crlf

*.jar binary
*.apk binary
*.keystore binary
