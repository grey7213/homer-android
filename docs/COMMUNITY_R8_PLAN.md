# 社区作为首页并公开入口

范围：Android 默认启动、登录回跳、现有主导航和社区公开配置；保留鉴权、协议、审核、禁言及本机数据隔离。不直接部署生产服务器。

1. 已完成：检查启动、导航、服务端开放开关与提交工作区。原生已指向社区，但网页根入口仍恢复旧对话、导航仍动态隐藏。
2. 已完成：统一社区首页；固定社区导航；debug 本机模式改为显式入口；新增/既有 internal 数据库一次性迁移 open，保留运维明确设置的 closed。
3. 已完成：验证新装/升级迁移、普通用户导航、服务不可用错误、无验收数据冒充线上；运行 63 项 API 检查、三视口浏览器全链路、Android 单测/Lint/构建和安装包设备检查。
4. 交付：导出当前 web 基线的累计补丁，将客户端、社区服务扩展、测试和说明整理到独立分支，通过 PR 提交到 main；生产服务器部署与正式签名发布仍由维护流程完成。

验收：普通账号无需管理员身份即可看到社区；正常启动不读取历史后自动跳对话；真实模式不自动切换本机；closed 紧急关闭与管理员权限仍有效；无凭据、APK、缓存或构建目录进入提交。
